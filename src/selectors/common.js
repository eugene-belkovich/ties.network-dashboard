export const routerSelector = x => x.router
export const userSelector = x => x.user
export const userStatusSelector = x => x.userStatus
export const contractSelector = x => x.contract
export const bitcoinSelector = x => x.bitcoin
export const transactionsSelector = x => x.transactions
export const referralsSelector = x => x.referrals
